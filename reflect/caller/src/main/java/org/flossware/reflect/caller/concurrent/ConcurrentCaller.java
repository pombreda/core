package org.flossware.reflect.caller.concurrent;

import org.flossware.reflect.caller.Caller;

/**
 *
 * Interface denoting support for concurrent calls.
 *
 * @author sfloess
 *
 */
public interface ConcurrentCaller<V> extends Caller<V> {
}
